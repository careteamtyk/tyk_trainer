import { useEffect } from "react"

const NumberHelper = (props)=>{
    const {socket, names, linkCode, setNames, nump, setNumP} = props
    
    return(
        <>

        </>
    )
}
export default NumberHelper